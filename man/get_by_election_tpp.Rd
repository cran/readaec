% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/by_elections.R
\name{get_by_election_tpp}
\alias{get_by_election_tpp}
\title{Get by-election two-party preferred votes by polling place}
\usage{
get_by_election_tpp(division, year = NULL, refresh = FALSE)
}
\arguments{
\item{division}{Division name (e.g. "Farrer").}

\item{year}{By-election year. Only needed where a division has had more
than one by-election (e.g. Mayo in 2008 and 2018).}

\item{refresh}{If \code{TRUE}, re-download from the AEC even if a cached copy
exists.}
}
\value{
A tidy data frame of TPP votes by polling place.
}
\description{
Note that TPP figures are only meaningful where the final two candidates
were ALP and Coalition. In by-elections decided between other candidates
(such as Farrer in 2026), use \code{\link[=get_by_election_tcp]{get_by_election_tcp()}} instead.
}
\examples{
\donttest{
op <- options(readaec.cache_dir = tempdir())
get_by_election_tpp("Dunkley")
options(op)
}
}
